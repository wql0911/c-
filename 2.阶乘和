#include<stdio.h>

int main()
{
	int n;	
	int sum = 0;
	int a = 1;
    
	printf("请输入一个1-20的数字");
	scanf("%d/n", &n);
	for (int i = 1; n==i; i++)
	{
		a = a * i;//计算阶乘
		sum = sum + a；
	}
  
  printf("%d", sum);
  
	return 0;
}
//程序出错，当输入值为2之后输出为0，若改为输出a则无输出（循环出错？）
//int a=a*i放在循环内会报错
