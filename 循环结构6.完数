#include<stdio.h>
 int main()
{
  int i,j,k,n;
  scanf("%d",&n);
  for(i=1;i<n;i++)
  {
  	int sum=0;
    for(j=1;j<i;j++)
     {
    	if(i%j==0)
    	 {
    		  sum=sum+j;
    	    if(sum==n)
    	    {
    	    	for(k=1;k<i;k++)
    	    	printf("%d ",k);
			    }
		    }
	    }
    }
  return 0;    
 }
 //有错
