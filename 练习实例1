#include <stdio.h>

int main()
{
	int sum = 0;
	for (int i = 1; i < 5; i++)
	{
		for (int j = 1; j < 5; j++)
		{
			for (int k = 1; k < 5; j++)
			{
				if (i != k && i != j && j != k)
				{
					printf("%d%d%d   ", i, j, k);
					sum = sum + 1;
				}
			}
		}
	}
	printf("%d", sum);
	return 0;
}
