#include<stdio.h>
int main()
{
	int a, b, c;
	scanf("%d %d %d", &a, &b, &c);
	if (a - b > 0)
	{
		if (b - c > 0)
			printf("%d", c);
		else
			printf("%d", b);
	}
	else
	{
		if (a - c > 0)
			printf("%d", c);
		else
			printf("%d", a);
	}
	return 0;
}
