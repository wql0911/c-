#include<stdio.h>
int main()
{
	int n, a=1, b=1,c=1;
	scanf("%d", &n);
	for(int i=3;i<=n;i++)
	{ 
		c =(a + b)%10007;
		a = b;
		b = c;
	}
	printf("%d", c);
	
	return 0;
}
//如果把取余放在输出函数中，当n数值大了之后会输出负数，两种输入方法的输出对比：50 4803 -3969
